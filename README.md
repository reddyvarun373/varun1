# varun1
sampleprogram
welcom to git and github
welcom to commit
Store input numbers from the user
num1 = input("Enter first number: ")
num2 = input("Enter second number: ")
Convert strings to floats so it supports decimals, then add them
total_sum = float(num1) + float(num2)

Display the result using an f-string
print(f"The sum of {num1} and {num2} is {total_sum}")
